

// const Selected = ({ selected }) => {
//     const { title } = selected;
//     return (
//         <div>
//             <h3>{title}</h3>
//         </div>
//     );
// };

// export default Selected;
const Selected = ({ selected }) => {
    const { title } = selected;
    return (
        <div>
            <h3>{title}</h3>
        </div>
    );
};

export default Selected;
